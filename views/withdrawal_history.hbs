{{> header}}
<div class="container-fluid">
  <div class="row navi">
    <div class="col-lg-3">
      {{> menu}}
    </div>
    <div class="col-lg-9">
      <div class="content">
        <!-- Withdraw History -->
        <div class="content__history--withdraw text-center">
          <h2 class="text-center text-light mt-4">Withdraw history</h2>
          <table class="table text-light">
            <thead class="thead-light">
              <tr>
                <th scope="col">Withdrawal ID</th>
                <th scope="col">Credit Card</th>
                <th scope="col">Fee</th>
                <th scope="col">Money</th>
                <th scope="col">Status</th>
                <th scope="col">Date</th>
              </tr>
            </thead>
            <tbody>
              {{#each withdrawalHistories}}
              <tr>
                <td>{{_id}}</td>
                <td>{{cardNumber}}</td>
                <td>{{formatCurrency fee}}</td>
                <td>{{formatCurrency money}}</td>
                <td>{{status}}</td>
                <td>{{date}}</td>
              </tr>
              {{/each}}
            </tbody>
          </table>
          <a href="/transaction_history" class="btn btn-light"><i class="fas fa-arrow-left"></i>
            Go back</a>
        </div>
      </div>
    </div>
  </div>
  {{> menu_responsive}}
</div>